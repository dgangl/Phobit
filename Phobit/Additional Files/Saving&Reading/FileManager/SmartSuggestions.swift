//
//  SmartSuggestions.swift
//  Phobit
//
//  Created by 73 on 12.03.18.
//  Copyright © 2018 73. All rights reserved.
//

import Foundation

extension Memory {

    
    func newStringForSearch(rechnungsersteller: String) {
        /*
        let key = "SmartSuggestions_Rechnungsersteller"
        
        if let anyArray = UserDefaults.standard.array(forKey: key) {
            var array = anyArray as! [String]
            
            if array.contains(rechnungsersteller) == false {
                array.append(rechnungsersteller)
            }
            
            UserDefaults.standard.set(array as Any, forKey: key)
            
        } else {
            let array = [rechnungsersteller]
            UserDefaults.standard.set(array as Any, forKey: key)
        }
         */
    }
    
}
